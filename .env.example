# --- Cloudflare 认证配置 ---
# 推荐：API Token（具备 Zone.Zone Read 与 Zone.DNS Edit 权限，作用于目标 Zone）
CF_API_TOKEN=

# 备用：Global API Key + Email（当 API Token 认证失败且你提供了这两个值时，程序会回退尝试）
CF_API_KEY=
CF_API_EMAIL=

# --- Cloudflare 目标配置 ---
# 目标 Zone 的 ID（必填）
CF_ZONE_ID=
# 要批量管理的 A 记录的域名（例如：cf.example.com）
CF_TARGET_DOMAIN=

# --- IP 来源配置 ---
# 逗号分隔的域名列表，用于解析出候选 IP（例如：a.example.com,b.example.com）
SOURCE_DOMAINS=

# --- 任务调度配置 ---
# 每轮执行的间隔秒数（默认 3600）
INTERVAL_SECONDS=3600

# --- 通知配置（可选） ---
# Bark 推送的基础 URL（例如：https://api.day.app/你的Key）
BARK_URL=

